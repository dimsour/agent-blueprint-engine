
# optional; this app's public origin, used for the GitHub OAuth redirect and for the origin the
# token is posted back to. Set it on a deployment that does not sit behind a proxy it trusts:
# without it, the forwarded host headers are believed.
APP_ORIGIN=
